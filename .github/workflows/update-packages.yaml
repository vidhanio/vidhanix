# This file is @generated by `just generate`, do not edit directly.
%YAML 1.1
---
concurrency:
  cancel-in-progress: false
  group: update-packages
jobs:
  update:
    name: 'Update Package: ${{ matrix.pkg }}'
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v5
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - env:
        PACKAGE: ${{ matrix.pkg }}
      id: update
      name: Update Package
      run: bash .github/scripts/update-package.sh "$PACKAGE"
    - id: app-token
      name: Create GitHub App Token
      uses: actions/create-github-app-token@v3
      with:
        client-id: ${{ vars.APP_CLIENT_ID }}
        private-key: ${{ secrets.APP_PRIVATE_KEY }}
    - name: Create Pull Request
      uses: peter-evans/create-pull-request@v8
      with:
        body: ${{ steps.update.outputs.body }}
        branch: packages/${{ matrix.pkg }}
        commit-message: 'chore(packages): bump ${{ matrix.pkg }} from ${{ steps.update.outputs.before
          }} to ${{ steps.update.outputs.after }}'
        delete-branch: true
        sign-commits: true
        title: 'chore(packages): bump ${{ matrix.pkg }} from ${{ steps.update.outputs.before
          }} to ${{ steps.update.outputs.after }}'
        token: ${{ steps.app-token.outputs.token }}
    strategy:
      fail-fast: false
      matrix:
        pkg:
        - bibata-cursor
        - breezex-cursor
        - conventional-pre-commit
        - google-cursor
name: Update Packages
'on':
  push:
    branches:
    - main
  schedule:
  - cron: 0 9 * * *
permissions:
  actions: write
  contents: write
  pull-requests: write
