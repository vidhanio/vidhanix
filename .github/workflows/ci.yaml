# This file is @generated by `just generate`, do not edit directly.
%YAML 1.1
---
concurrency:
  cancel-in-progress: true
  group: ${{ github.ref }}
jobs:
  build-packages:
    name: 'Build Package: ${{ matrix.pkg }}'
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v5
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - name: Build Package
      run: nix build .#${{ matrix.pkg }} --print-build-logs
    strategy:
      fail-fast: false
      matrix:
        pkg:
        - berkeley-mono
        - berkeley-mono-variable
        - breezex-combined
        - breezex-cursor
        - breezex-hyprcursor
        - conventional-pre-commit
        - generate-files
        - google-sans-flex
        - pi-context-view
        - pi-subagents
        - pi-web-access
        - pragmata-pro-variable
        - update-packages
        - write-flake
        - write-inputs
        - write-lock
  check-formatting:
    name: Check Formatting
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v5
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - name: Check Formatting
      run: nix develop -c just fmt --ci
  check-generated-files:
    name: Check Generated Files
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v5
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - name: Generate Files
      run: nix develop -c just generate
    - name: Check Diff
      run: git diff --exit-code
  eval-systems:
    name: 'Evaluate System: ${{ matrix.name }}'
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v5
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - name: Evaluate System
      run: nix eval .#${{ matrix.attr }} --raw
    strategy:
      fail-fast: false
      matrix:
        include:
        - attr: nixosConfigurations.vortex.config.system.build.toplevel.drvPath
          name: vortex
        - attr: nixosConfigurations.voyager.config.system.build.toplevel.drvPath
          name: voyager
name: CI
'on':
  pull_request: {}
  push: {}
permissions:
  actions: write
  contents: read
