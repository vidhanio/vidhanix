# This file is @generated by `just generate`, do not edit directly.
%YAML 1.1
---
jobs:
  build-packages:
    concurrency:
      cancel-in-progress: false
      group: ${{ format('nix-{0}', matrix.drvPath) }}
      queue: max
    name: 'Build Package (${{ matrix.system }}): ${{ matrix.pkg }}'
    needs: plan-builds
    runs-on: ${{ matrix.runner }}
    steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        cachix-auth-token: ${{ ((github.ref == 'refs/heads/main' && 
          (github.event_name == 'push' || github.event_name == 'schedule' || 
          github.event_name == 'workflow_dispatch')) || (github.event_name == 
          'push' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.actor)) || (github.event_name == 'pull_request' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.event.pull_request.user.login))) && secrets.CACHIX_AUTH_TOKEN 
          || '' }}
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - name: Build Package
      run: nix build .#${{ matrix.pkg }} --print-build-logs
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.plan-builds.outputs.packages) }}
  build-systems:
    concurrency:
      cancel-in-progress: false
      group: ${{ format('nix-{0}', matrix.drvPath) }}
      queue: max
    name: 'Build System (${{ matrix.system }}): ${{ matrix.name }}'
    needs: plan-builds
    runs-on: ${{ matrix.runner }}
    steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        cachix-auth-token: ${{ ((github.ref == 'refs/heads/main' && 
          (github.event_name == 'push' || github.event_name == 'schedule' || 
          github.event_name == 'workflow_dispatch')) || (github.event_name == 
          'push' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.actor)) || (github.event_name == 'pull_request' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.event.pull_request.user.login))) && secrets.CACHIX_AUTH_TOKEN 
          || '' }}
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - name: Build System
      run: nix build .#${{ matrix.attr }} --print-build-logs
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.plan-builds.outputs.systems) }}
  check-formatting:
    name: Check Formatting
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        cachix-auth-token: ${{ ((github.ref == 'refs/heads/main' && 
          (github.event_name == 'push' || github.event_name == 'schedule' || 
          github.event_name == 'workflow_dispatch')) || (github.event_name == 
          'push' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.actor)) || (github.event_name == 'pull_request' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.event.pull_request.user.login))) && secrets.CACHIX_AUTH_TOKEN 
          || '' }}
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - name: Check Formatting
      run: nix develop -c just fmt --ci
  check-generated-files:
    name: Check Generated Files
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        cachix-auth-token: ${{ ((github.ref == 'refs/heads/main' && 
          (github.event_name == 'push' || github.event_name == 'schedule' || 
          github.event_name == 'workflow_dispatch')) || (github.event_name == 
          'push' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.actor)) || (github.event_name == 'pull_request' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.event.pull_request.user.login))) && secrets.CACHIX_AUTH_TOKEN 
          || '' }}
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - name: Generate Files
      run: nix develop -c just generate
    - name: Check Diff
      run: git diff --exit-code
  plan-builds:
    name: Plan Builds
    outputs:
      packages: ${{ steps.build-matrices.outputs.packages }}
      systems: ${{ steps.build-matrices.outputs.systems }}
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        cachix-auth-token: ''
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - id: build-matrices
      name: Build Matrices
      run: "printf 'packages=%s\\n' \"$(nix eval --json .#ciBuildMatrices.packages)\"\
        \ >> \"$GITHUB_OUTPUT\"\nprintf 'systems=%s\\n' \"$(nix eval --json .#ciBuildMatrices.systems)\"\
        \ >> \"$GITHUB_OUTPUT\"\n"
name: CI
'on':
  pull_request: {}
  push: {}
permissions:
  actions: write
  contents: read
