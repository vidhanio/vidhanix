# This file is @generated by `just generate`, do not edit directly.
%YAML 1.1
---
concurrency:
  cancel-in-progress: true
  group: ${{ github.ref }}
jobs:
  build-packages:
    secrets:
      FONTS_SSH_KEY: ${{ secrets.FONTS_SSH_KEY }}
    strategy:
      fail-fast: false
      matrix:
        pkg:
        - berkeley-mono
        - berkeley-mono-variable
        - breezex-combined
        - breezex-cursor
        - breezex-hyprcursor
        - conventional-pre-commit
        - generate-files
        - google-sans-flex
        - helium-bin
        - hindsight
        - hindsight-api-slim
        - libretro-database
        - libretro-system-files
        - libretrodb-tool
        - pragmata-pro-variable
        - update-packages
        - write-flake
        - write-inputs
        - write-lock
    uses: ./.github/workflows/nix.yaml
    with:
      command: nix build .#${{ matrix.pkg }} --print-build-logs
      name: 'build package: ${{ matrix.pkg }}'
  check-formatting:
    secrets:
      FONTS_SSH_KEY: ${{ secrets.FONTS_SSH_KEY }}
    uses: ./.github/workflows/nix.yaml
    with:
      command: nix develop -c just fmt --ci
      name: check formatting
  check-generated-files:
    secrets:
      FONTS_SSH_KEY: ${{ secrets.FONTS_SSH_KEY }}
    uses: ./.github/workflows/nix.yaml
    with:
      command: nix develop -c just generate && git diff --exit-code
      name: check generated files
  eval-systems:
    secrets:
      FONTS_SSH_KEY: ${{ secrets.FONTS_SSH_KEY }}
    strategy:
      fail-fast: false
      matrix:
        include:
        - attr: 
            nixosConfigurations.vidhan-macbook.config.system.build.toplevel.drvPath
          name: vidhan-macbook
        - attr: 
            nixosConfigurations.vidhan-pc.config.system.build.toplevel.drvPath
          name: vidhan-pc
    uses: ./.github/workflows/nix.yaml
    with:
      command: nix eval .#${{ matrix.attr }} --raw
      name: 'eval system: ${{ matrix.name }}'
name: ci
'on':
  pull_request: {}
  push: {}
permissions:
  actions: write
  contents: read
