# This file is @generated by `just generate`, do not edit directly.
%YAML 1.1
---
jobs:
  build-packages:
    concurrency:
      cancel-in-progress: false
      group: ${{ format('nix-{0}', matrix.pkg) }}
      queue: max
    name: 'Build Package (${{ matrix.system }}): ${{ matrix.pkg }}'
    runs-on: ${{ matrix.runner }}
    steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        cachix-auth-token: ${{ ((github.ref == 'refs/heads/main' && 
          (github.event_name == 'push' || github.event_name == 'schedule' || 
          github.event_name == 'workflow_dispatch')) || (github.event_name == 
          'push' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.actor)) || (github.event_name == 'pull_request' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.event.pull_request.user.login))) && secrets.CACHIX_AUTH_TOKEN 
          || '' }}
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - name: Build Package
      run: nix build .#${{ matrix.pkg }} --print-build-logs
    strategy:
      fail-fast: false
      matrix:
        include:
        - pkg: berkeley-mono
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: berkeley-mono-variable
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: bibata-combined
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: bibata-cursor
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: bibata-hyprcursor
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: breezex-combined
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: breezex-cursor
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: breezex-hyprcursor
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: cider
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: generate-files
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: google-combined
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: google-cursor
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: google-hyprcursor
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: google-sans-flex
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: linux-asahi-fairydust
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: muvm-steam
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: pragmata-pro-variable
          runner: ubuntu-26.04-arm
          system: aarch64-linux
        - pkg: berkeley-mono
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: berkeley-mono-variable
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: bibata-combined
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: bibata-cursor
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: bibata-hyprcursor
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: breezex-combined
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: breezex-cursor
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: breezex-hyprcursor
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: cider
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: generate-files
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: google-combined
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: google-cursor
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: google-hyprcursor
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: google-sans-flex
          runner: ubuntu-latest
          system: x86_64-linux
        - pkg: pragmata-pro-variable
          runner: ubuntu-latest
          system: x86_64-linux
  build-systems:
    concurrency:
      cancel-in-progress: false
      group: ${{ format('nix-{0}', matrix.name) }}
      queue: max
    name: 'Build System (${{ matrix.system }}): ${{ matrix.name }}'
    runs-on: ${{ matrix.runner }}
    steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        cachix-auth-token: ${{ ((github.ref == 'refs/heads/main' && 
          (github.event_name == 'push' || github.event_name == 'schedule' || 
          github.event_name == 'workflow_dispatch')) || (github.event_name == 
          'push' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.actor)) || (github.event_name == 'pull_request' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.event.pull_request.user.login))) && secrets.CACHIX_AUTH_TOKEN 
          || '' }}
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - name: Build System
      run: nix build .#${{ matrix.attr }} --print-build-logs
    strategy:
      fail-fast: false
      matrix:
        include:
        - attr: nixosConfigurations.vortex.config.system.build.toplevel
          name: vortex
          runner: ubuntu-latest
          system: x86_64-linux
        - attr: nixosConfigurations.voyager.config.system.build.toplevel
          name: voyager
          runner: ubuntu-26.04-arm
          system: aarch64-linux
  check-formatting:
    name: Check Formatting
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        cachix-auth-token: ${{ ((github.ref == 'refs/heads/main' && 
          (github.event_name == 'push' || github.event_name == 'schedule' || 
          github.event_name == 'workflow_dispatch')) || (github.event_name == 
          'push' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.actor)) || (github.event_name == 'pull_request' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.event.pull_request.user.login))) && secrets.CACHIX_AUTH_TOKEN 
          || '' }}
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - name: Check Formatting
      run: nix develop -c just fmt --ci
  check-generated-files:
    name: Check Generated Files
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Set Up Nix
      uses: ./.github/actions/setup-nix
      with:
        cachix-auth-token: ${{ ((github.ref == 'refs/heads/main' && 
          (github.event_name == 'push' || github.event_name == 'schedule' || 
          github.event_name == 'workflow_dispatch')) || (github.event_name == 
          'push' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.actor)) || (github.event_name == 'pull_request' && 
          contains(fromJSON('["vidhanio","vidhanix[bot]","dependabot[bot]"]'), 
          github.event.pull_request.user.login))) && secrets.CACHIX_AUTH_TOKEN 
          || '' }}
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - name: Generate Files
      run: nix develop -c just generate
    - name: Check Diff
      run: git diff --exit-code
name: CI
'on':
  pull_request: {}
  push: {}
permissions:
  actions: write
  contents: read
