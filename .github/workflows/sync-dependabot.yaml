# This file is @generated by `just generate`, do not edit directly.
%YAML 1.1
---
concurrency:
  cancel-in-progress: true
  group: ${{ github.event.pull_request.number }}
jobs:
  metadata:
    if: github.event.pull_request.user.login=='dependabot[bot]'
    name: fetch dependabot metadata
    outputs:
      dependencies: ${{ steps.metadata.outputs.updated-dependencies-json }}
      ecosystem: ${{ steps.metadata.outputs.package-ecosystem }}
    runs-on: ubuntu-latest
    steps:
    - id: metadata
      name: fetch dependabot metadata
      uses: dependabot/fetch-metadata@v3
  prune-lock:
    if: needs.metadata.outputs.ecosystem=='nix'
    name: prune lock
    needs:
    - metadata
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v5
      with:
        fetch-depth: 0
        ref: ${{ github.event.pull_request.head.sha }}
    - name: setup nix
      uses: ./.github/actions/setup-nix
      with:
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - id: app-token
      name: create github app token
      uses: actions/create-github-app-token@v3
      with:
        client-id: ${{ vars.APP_CLIENT_ID }}
        private-key: ${{ secrets.APP_PRIVATE_KEY }}
    - name: prune lock
      run: nix develop -c just generate
    - env:
        GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
      name: commit
      uses: planetscale/ghcommit-action@v0.2.22
      with:
        branch: ${{ github.event.pull_request.head.ref }}
        commitMessage: 'chore(flake): prune lock'
        repo: ${{ github.repository }}
  sync-actions:
    if: needs.metadata.outputs.ecosystem=='github_actions'
    name: sync action updates
    needs:
    - metadata
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v5
      with:
        fetch-depth: 0
        ref: ${{ github.event.pull_request.base.sha }}
    - env:
        UPDATED_DEPENDENCIES_JSON: ${{ needs.metadata.outputs.dependencies }}
      name: sync action updates
      run: python3 .github/scripts/sync-action-updates.py
    - id: app-token
      name: create github app token
      uses: actions/create-github-app-token@v3
      with:
        client-id: ${{ vars.APP_CLIENT_ID }}
        private-key: ${{ secrets.APP_PRIVATE_KEY }}
    - env:
        GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
      name: commit
      uses: planetscale/ghcommit-action@v0.2.22
      with:
        branch: ${{ github.event.pull_request.head.ref }}
        commitMessage: 'chore(ci): sync action updates to nix'
        repo: ${{ github.repository }}
name: sync dependabot
'on':
  pull_request_target:
    branches:
    - main
    types:
    - opened
    - synchronize
    - reopened
permissions:
  actions: write
  contents: write
  pull-requests: write
