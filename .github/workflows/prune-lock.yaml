# This file is @generated by `just generate`, do not edit directly.
%YAML 1.1
---
jobs:
  prune:
    if: github.event.pull_request.user.login=='dependabot[bot]' && 
      startsWith(github.event.pull_request.head.ref,'dependabot/nix/')
    name: prune lock
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v5
      with:
        fetch-depth: 0
        ref: ${{ github.event.pull_request.head.sha }}
    - name: setup nix
      uses: ./.github/actions/setup-nix
      with:
        ssh-private-key: ${{ secrets.FONTS_SSH_KEY }}
    - id: app-token
      name: create github app token
      uses: actions/create-github-app-token@v3
      with:
        client-id: ${{ vars.APP_CLIENT_ID }}
        private-key: ${{ secrets.APP_PRIVATE_KEY }}
    - if: github.event.sender.login != concat(steps.app-token.outputs.app-slug, 
        '[bot]')
      name: prune lock
      run: nix develop -c just generate
    - env:
        GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
      if: github.event.sender.login != concat(steps.app-token.outputs.app-slug, 
        '[bot]')
      name: commit
      uses: planetscale/ghcommit-action@v0.2.22
      with:
        branch: ${{ github.event.pull_request.head.ref }}
        commit_message: 'chore(deps): prune lock after nix bump'
        repo: ${{ github.repository }}
name: prune lock
'on':
  pull_request_target:
    branches:
    - main
    types:
    - opened
    - synchronize
    - reopened
permissions:
  actions: write
  contents: write
  pull-requests: write
